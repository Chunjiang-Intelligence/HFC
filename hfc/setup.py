from setuptools import setup, find_packages

setup(
    name="hfc_trainer",
    version="0.1.0",
    packages=find_packages(),
    description="Hierarchical Federated Clustering (HFC) Trainer for Large Language Models",
    python_requires='>=3.10',
)
